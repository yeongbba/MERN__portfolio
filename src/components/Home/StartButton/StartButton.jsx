import React from "react";
import styles from "./StartButton.module.css";

function StartButton() {
  return (
    <div>
      <button className={styles.bigButton}>PORTFOLIO START</button>
    </div>
  );
}

export default StartButton;
