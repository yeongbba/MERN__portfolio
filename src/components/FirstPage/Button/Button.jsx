import React from "react";
import styles from "./Button.module.css";

function Button() {
  return (
    <div className={`${styles.btn} ${styles.color0}`}>PORTFOLIO START</div>
  );
}

export default Button;
